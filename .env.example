# Copy this file to `.env` and paste in your real Google OAuth Client ID.
# Get one at: https://console.cloud.google.com/apis/credentials
# The VITE_ prefix is required so Vite exposes the variable to the browser.
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
